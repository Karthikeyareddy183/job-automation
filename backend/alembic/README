Generic single-database configuration for Job Automation System.

To create a new migration:
    alembic revision --autogenerate -m "description of changes"

To apply migrations:
    alembic upgrade head

To downgrade one revision:
    alembic downgrade -1

To view migration history:
    alembic history

To see current revision:
    alembic current
